import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Que cule viaje vale",
  description: "SEO Title",
};

export default function AboutPage() {
  return <span className="text-7xl">About Page</span>;
}
